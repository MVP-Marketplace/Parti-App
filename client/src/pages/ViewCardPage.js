import React from 'react';
import TopNavBar from '../components/TopNavBar';

//page for recipient to view the card that they were sent by an organizer
//TODO: complete components for ViewCard but we need to complete user authentication and contributor card edits and such before this can be completed.
function ViewCardPage() {
	return (
		<div>
			<TopNavBar />
		</div>
	);
}

export default ViewCardPage;
